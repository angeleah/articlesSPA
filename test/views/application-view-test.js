describe('App.Views.Application', function() {
  beforeEach(function() {
  });

  afterEach(function() {
  });

  it('creates an ArticlesCollection view', function() {
    expect(true).to.be.true;
  });
});
